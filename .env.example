# Plantilla de variables de entorno. Copiar a `.env` (gitignored) y rellenar.
# El proyecto NO tiene defaults; spring-dotenv falla si faltan estas variables.

DB_USERNAME=postgres
DB_PASSWORD=cambiar-en-local