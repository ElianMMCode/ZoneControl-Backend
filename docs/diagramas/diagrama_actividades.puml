@startuml
skinparam activity {
  BackgroundColor LightCyan
  BorderColor DarkSlateGray
}
skinparam swimlane {
  BorderColor DarkSlateGray
  BorderThickness 2
}

|Frontend (React / Vue)|
start
:Ingresar código de empleado;
:Enviar petición (POST /access/validate);

|Backend (Spring Boot)|
:Recibir petición;
:Consultar estado del empleado;

|Base de Datos (PostgreSQL)|
:Buscar empleado por código;

|Backend (Spring Boot)|
if (¿El empleado existe?) then (Sí)
  if (¿Estado == 'Autorizado'?) then (Sí)
    :Establecer resultado: "Ingreso Autorizado";
  else (No)
    :Establecer resultado: "Ingreso Denegado";
  endif
else (No)
  :Establecer resultado: "No Registrado";
endif

:Construir objeto de auditoría (Log);

|Base de Datos (PostgreSQL)|
:Insertar registro en tabla Historial;

|Backend (Spring Boot)|
:Retornar código HTTP y mensaje;

|Frontend (React / Vue)|
:Renderizar alerta visual en pantalla;
stop
@enduml
